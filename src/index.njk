---
layout: layouts/base.njk
title: false
permalink: /index.html
---
<div class="landing">
  <div class="hero">
    {# 프롬프트 줄은 ASCII만 둔다 — 한글을 넣으면 모노스페이스 폴백으로 글꼴이 깨진다. #}
    <p class="promptline"><b>❯</b> <span class="typed">claude</span><span class="caret" aria-hidden="true"></span></p>
    <h1>{{ site.title }}</h1>
    <p class="lede">
      기능 이름이 기억나지 않아도 된다. 하고 싶은 일을 그대로 치면 된다 —
      「커밋 전에 자동으로」, 「승인 창 그만」, 「되돌리기」.
    </p>
  </div>

  {% include "partials/searchbox.njk" %}

  <p class="meta-line">
    레퍼런스 {{ stats.itemCount }}개 · 작업 {{ taskIndex.all | count }}개 · 프롬프트 {{ stats.promptCount }}개 —
    <a href="/about/">이 사이트가 지금 어느 정도인가</a>
  </p>
</div>
